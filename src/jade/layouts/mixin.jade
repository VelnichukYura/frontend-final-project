//- Declaration
mixin product(image, title, price, label)
  div.product
    div.image
      if(label)
        span label
      img(src=image)
    div.title
      h3= title
    div.price
      span= price
